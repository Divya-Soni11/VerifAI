chatgpt how to install nextapp
make sure when building nextapp select tailwind with it 
npm install 
recharts
framer-motion
lucide-react

and if some libray remain read the error message and install it 
Npm run dev

all the data are in the components of the nextapp 
